#include <iostream>
// Include the cmath library
#include <cmath>
using namespace std;

int sum(int a, int b) {
    return a + b;
}

int main() {
    const int kN = 5;
    int a;
    if (a = 0) {
        cout << "a = 0" << std::endl;
    } else {
        cout << "! a = 0" << std::endl;
    }
    cout << "kN " << kN << std::endl;

    cout << sum(64, 42);

    cout << sqrt(64);
    cout << round(2.6);
    cout << log(2);

    cout << M_PI;
    return 0;
}
